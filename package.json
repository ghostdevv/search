{
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "build": "worktop build src/index.ts",
    "dev": "miniflare -w"
  },
  "dependencies": {
    "worktop": "next"
  },
  "devDependencies": {
    "miniflare": "^2.4.0",
    "typescript": "^4.6.3",
    "worktop.build": "latest"
  }
}